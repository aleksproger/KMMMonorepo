package Multiplatform.Architecture

// class DefaultReducer<State, in Action, out Effect>(
//     private val state: State?,
//     private val reducer: Reducer<State, Action, Effect>
// ) : Reducer<State, Action, Effect> {
//     override fun reduce(state: State, action: Action): Pair<State, Effect> {
        
//     }
// } 