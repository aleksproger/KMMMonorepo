---
# Шаблон Xcodegen для таргета Acceptance Tests.
# Шаблон принимает:
# - Source файлы из выбранной директории с названием таргета в подпапке _AcceptanceTests_.
# Пример:
# Target: SDFramework. Исходные файлы будут браться из
# Sources/SDFramework/Foo/_AcceptanceTests_/FooTests.swift.
# Также для таргета автоматически проставляется зависимость на основной таргет.
# Пример: FooFrameworkTests -> FooFramework.
include:
  - path: build-settings/unit_tests_settings.yml
    relativePaths: false

targetTemplates:
  AcceptanceTests:
    type: bundle.unit-test
    platform: iOS

    settings:
      groups:
        - UnitTests

    sources:
      - path: Sources/${testingTarget}
        includes:
          - "**/_AcceptanceTests_"

    dependencies:
      - target: ${testingTarget}
        embed: false
        link: true

    buildImplicitDependencies: false
